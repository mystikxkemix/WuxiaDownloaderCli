{
    "name": "wuxia-downloader-cli",
    "version": "0.0.1",
    "scripts": {
        "start": "npm run build && node dist/index.js",
        "build": "tsc"
    },
    "devDependencies": {
        "@types/prompt": "^1.1.2",
        "typescript": "^4.5.5"
    },
    "dependencies": {
        "epub-gen": "^0.1.0",
        "prompt": "^1.2.2",
        "puppeteer": "^13.4.0"
    }
}
